app.controller('ChartController', ['$scope', function ($scope) {
   
    //draws a line chart
    $scope.initChart = function () {
        //something will go here, but for now all logic has been moved to the draw-line directive
    }
}]);
